# Zoho Calendar OAuth
ZOHO_CLIENT_ID=
ZOHO_CLIENT_SECRET=
ZOHO_REDIRECT_URI=https://bookings.buildark.dev/api/zoho/callback

# Resend (email)
RESEND_API_KEY=
EMAIL_FROM=raz@buildark.dev

# Admin
ADMIN_PASSWORD=changeme
ADMIN_EMAIL=raz@buildark.dev

# App
NEXT_PUBLIC_APP_URL=https://bookings.buildark.dev

# Zoho tokens — set these manually on Vercel after first OAuth flow
# (locally, tokens are stored in .zoho-tokens.json instead)
ZOHO_ACCESS_TOKEN=
ZOHO_REFRESH_TOKEN=
ZOHO_TOKEN_EXPIRES_AT=
ZOHO_CALENDAR_ID=
